import { PipeFilterArrayImpurePipe } from './pipe-filter-array-impure.pipe';

describe('PipeFilterArrayImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFilterArrayImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
