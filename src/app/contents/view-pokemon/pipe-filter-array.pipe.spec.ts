import { PipeFilterArrayPipe } from './pipe-filter-array.pipe';

describe('PipeFilterArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFilterArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
